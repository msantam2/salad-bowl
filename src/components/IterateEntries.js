import React from 'react';

class IterateEntries extends React.Component {
  constructor(props) {
    super(props);
    this.completedEntries = [];
    this.keepInBowl = [];
  }

  render() {
    const { entries } = this.props;

    return (
      <div>

      </div>
    );
  }
};

export default IterateEntries;
